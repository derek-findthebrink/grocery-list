// Generated by CoffeeScript 1.9.3
(function() {
  define(['views/widgets/ingredient'], function(ingredient) {
    var groceryList;
    return groceryList = Backbone.View.extend({
      el: '.groceries-list',
      addAll: function() {
        this.$el.html('');
        return this.collection.each(this.addOne, this);
      },
      addOne: function(item) {
        var view;
        view = new ingredient({
          model: item
        });
        return this.$el.find('ul').append(view.render().el);
      },
      initialize: function() {
        console.log('grocery list init');
        this.collection = App.ingredients;
        return this.addAll();
      }
    });
  });

}).call(this);
